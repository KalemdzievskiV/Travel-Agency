Editorial content card for stories, guides and inspiration tiles — image on top, eyebrow + serif title + body on paper.

```jsx
<Card
  image="/photos/dolomites.jpg"
  eyebrow="Slow Travel"
  title="The case for going slowly"
  body="Linger longer where it matters."
  footer={<Button variant="link">Read the story</Button>}
/>
```

Omit `image` for a placeholder. Pairs with `Button` in `footer`.
