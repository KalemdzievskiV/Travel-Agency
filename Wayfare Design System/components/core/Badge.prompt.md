Compact uppercase marker for status or category (e.g. "Guest favourite", "New").

```jsx
<Badge tone="coral">Trending</Badge>
<Badge tone="neutral" variant="solid">Sold out</Badge>
```

Tones: `neutral | coral | success | warning`. Variants: `soft` (tinted) or `solid` (filled).
