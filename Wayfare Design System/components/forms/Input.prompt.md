Labelled text field for forms and enquiry flows.

```jsx
<Input label="Full name" placeholder="Jane Appleseed" />
<Input label="Email" type="email" variant="underline" error="Required" />
```

Variants: `box` (bordered, default) and `underline` (editorial hairline). Props: `error`, `disabled`, `iconLeft`.
